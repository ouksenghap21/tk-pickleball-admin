<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>TK Pickleball Admin</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background: #f5f5f5;
            color: #333;
            min-height: 100vh;
        }

        /* ============================================
           LOGIN SCREEN
           ============================================ */
        .login-screen {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
        }

        .login-box {
            background: white;
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            max-width: 350px;
            width: 100%;
        }

        .login-logo {
            font-size: 48px;
            margin-bottom: 10px;
        }

        .login-title {
            font-size: 24px;
            font-weight: 700;
            margin-bottom: 8px;
            color: #333;
        }

        .login-subtitle {
            font-size: 14px;
            color: #666;
            margin-bottom: 30px;
        }

        .pin-display {
            display: flex;
            justify-content: center;
            gap: 12px;
            margin-bottom: 30px;
        }

        .pin-dot {
            width: 20px;
            height: 20px;
            border-radius: 50%;
            border: 2px solid #ddd;
            background: white;
            transition: all 0.2s;
        }

        .pin-dot.filled {
            background: #667eea;
            border-color: #667eea;
        }

        .pin-dot.error {
            background: #ff4444;
            border-color: #ff4444;
            animation: shake 0.5s;
        }

        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            75% { transform: translateX(5px); }
        }

        .pin-pad {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 12px;
            max-width: 250px;
            margin: 0 auto;
        }

        .pin-btn {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            border: none;
            background: #f5f5f5;
            font-size: 24px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .pin-btn:hover {
            background: #e0e0e0;
        }

        .pin-btn:active {
            transform: scale(0.95);
            background: #d0d0d0;
        }

        .pin-btn.clear {
            background: #ffebee;
            color: #c62828;
        }

        .pin-btn.submit {
            background: #e8f5e9;
            color: #2e7d32;
        }

        .login-error {
            color: #c62828;
            font-size: 14px;
            margin-top: 20px;
            display: none;
        }

        .login-error.show {
            display: block;
        }

        /* ============================================
           MAIN DASHBOARD
           ============================================ */
        .dashboard {
            display: none;
        }

        .dashboard.show {
            display: block;
        }

        /* Header */
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 16px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header-title {
            font-size: 20px;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .header-staff {
            font-size: 13px;
            opacity: 0.9;
        }

        .logout-btn {
            background: rgba(255,255,255,0.2);
            border: none;
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 13px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .logout-btn:hover {
            background: rgba(255,255,255,0.3);
        }

        /* Navigation Tabs */
        .nav-tabs {
            display: flex;
            background: white;
            border-bottom: 1px solid #e0e0e0;
            overflow-x: auto;
            position: sticky;
            top: 60px;
            z-index: 99;
        }

        .nav-tab {
            flex: 1;
            padding: 16px 12px;
            text-align: center;
            border: none;
            background: white;
            font-size: 13px;
            font-weight: 600;
            color: #666;
            cursor: pointer;
            border-bottom: 3px solid transparent;
            transition: all 0.3s;
            min-width: 80px;
        }

        .nav-tab:hover {
            background: #f5f5f5;
        }

        .nav-tab.active {
            color: #667eea;
            border-bottom-color: #667eea;
        }

        .nav-tab-icon {
            font-size: 20px;
            display: block;
            margin-bottom: 4px;
        }

        /* Content Sections */
        .content-section {
            display: none;
            padding: 20px;
            max-width: 1200px;
            margin: 0 auto;
        }

        .content-section.active {
            display: block;
        }

        /* Cards */
        .card {
            background: white;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 16px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
        }

        .card-title {
            font-size: 18px;
            font-weight: 700;
            color: #333;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 16px;
            margin-bottom: 20px;
        }

        .stat-card {
            background: white;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
        }

        .stat-value {
            font-size: 32px;
            font-weight: 700;
            color: #667eea;
        }

        .stat-label {
            font-size: 13px;
            color: #666;
            margin-top: 4px;
        }

        /* Date Picker */
        .date-picker-row {
            display: flex;
            gap: 12px;
            align-items: center;
            flex-wrap: wrap;
            margin-bottom: 16px;
        }

        .date-input {
            padding: 12px 16px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
            min-width: 150px;
        }

        .quick-btn {
            padding: 10px 20px;
            border: 1px solid #667eea;
            background: white;
            color: #667eea;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .quick-btn:hover, .quick-btn.active {
            background: #667eea;
            color: white;
        }

        /* Booking List */
        .booking-list {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .booking-item {
            background: white;
            border-radius: 12px;
            padding: 16px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 12px;
        }

        .booking-time {
            background: #667eea;
            color: white;
            padding: 8px 16px;
            border-radius: 8px;
            font-weight: 700;
            font-size: 16px;
            min-width: 90px;
            text-align: center;
        }

        .booking-info {
            flex: 1;
            min-width: 200px;
        }

        .booking-name {
            font-weight: 700;
            font-size: 16px;
            margin-bottom: 4px;
        }

        .booking-details {
            font-size: 13px;
            color: #666;
        }

        .booking-meta {
            display: flex;
            gap: 8px;
            align-items: center;
        }

        .booking-badge {
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
        }

        .badge-credits {
            background: #e8f5e9;
            color: #2e7d32;
        }

        .badge-cash {
            background: #fff3e0;
            color: #ef6c00;
        }

        .badge-cancelled {
            background: #ffebee;
            color: #c62828;
        }

        .booking-actions {
            display: flex;
            gap: 8px;
        }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-primary {
            background: #667eea;
            color: white;
        }

        .btn-primary:hover {
            background: #5a6fd6;
        }

        .btn-danger {
            background: #ffebee;
            color: #c62828;
        }

        .btn-danger:hover {
            background: #ffcdd2;
        }

        .btn-success {
            background: #e8f5e9;
            color: #2e7d32;
        }

        .btn-success:hover {
            background: #c8e6c9;
        }

        .btn-secondary {
            background: #f5f5f5;
            color: #333;
        }

        .btn-secondary:hover {
            background: #e0e0e0;
        }

        .btn-large {
            padding: 16px 32px;
            font-size: 16px;
            width: 100%;
        }

        /* Search Box */
        .search-box {
            display: flex;
            gap: 12px;
            margin-bottom: 20px;
        }

        .search-input {
            flex: 1;
            padding: 14px 20px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 16px;
            transition: border-color 0.3s;
        }

        .search-input:focus {
            outline: none;
            border-color: #667eea;
        }

        .search-btn {
            padding: 14px 24px;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.3s;
        }

        .search-btn:hover {
            background: #5a6fd6;
        }

        /* Customer Card */
        .customer-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 20px;
        }

        .customer-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 16px;
        }

        .customer-name {
            font-size: 24px;
            font-weight: 700;
        }

        .customer-phone {
            font-size: 14px;
            opacity: 0.9;
            margin-top: 4px;
        }

        .customer-balance {
            text-align: right;
        }

        .customer-balance-label {
            font-size: 12px;
            opacity: 0.8;
        }

        .customer-balance-value {
            font-size: 32px;
            font-weight: 700;
        }

        .customer-status {
            display: inline-block;
            padding: 4px 12px;
            background: rgba(255,255,255,0.2);
            border-radius: 20px;
            font-size: 12px;
            margin-top: 8px;
        }

        /* Package Grid */
        .package-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            gap: 12px;
            margin-bottom: 20px;
        }

        .package-card {
            background: white;
            border: 2px solid #e0e0e0;
            border-radius: 12px;
            padding: 20px;
            text-align: center;
            cursor: pointer;
            transition: all 0.3s;
        }

        .package-card:hover {
            border-color: #667eea;
            transform: translateY(-2px);
        }

        .package-card.selected {
            border-color: #667eea;
            background: rgba(102, 126, 234, 0.1);
        }

        .package-name {
            font-size: 14px;
            font-weight: 700;
            margin-bottom: 8px;
        }

        .package-hours {
            font-size: 28px;
            font-weight: 700;
            color: #667eea;
        }

        .package-hours-label {
            font-size: 12px;
            color: #666;
        }

        .package-price {
            font-size: 18px;
            font-weight: 700;
            color: #333;
            margin-top: 8px;
        }

        /* Form Groups */
        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            font-size: 14px;
            font-weight: 600;
            margin-bottom: 8px;
            color: #333;
        }

        .form-input, .form-select {
            width: 100%;
            padding: 14px 16px;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            font-size: 16px;
            transition: border-color 0.3s;
        }

        .form-input:focus, .form-select:focus {
            outline: none;
            border-color: #667eea;
        }

        .form-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 16px;
        }

        /* Summary Box */
        .summary-box {
            background: #f5f5f5;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .summary-row {
            display: flex;
            justify-content: space-between;
            padding: 8px 0;
            border-bottom: 1px solid #e0e0e0;
        }

        .summary-row:last-child {
            border-bottom: none;
            font-weight: 700;
            font-size: 18px;
            padding-top: 12px;
        }

        /* Customer List */
        .customer-list {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .customer-list-item {
            background: white;
            border-radius: 12px;
            padding: 16px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            display: flex;
            justify-content: space-between;
            align-items: center;
            cursor: pointer;
            transition: transform 0.2s;
        }

        .customer-list-item:hover {
            transform: translateX(4px);
        }

        .customer-list-info {
            flex: 1;
        }

        .customer-list-name {
            font-weight: 700;
            font-size: 16px;
        }

        .customer-list-phone {
            font-size: 13px;
            color: #666;
        }

        .customer-list-balance {
            text-align: right;
        }

        .customer-list-balance-value {
            font-size: 20px;
            font-weight: 700;
            color: #667eea;
        }

        .customer-list-balance-label {
            font-size: 12px;
            color: #666;
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.5);
            z-index: 1000;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .modal.show {
            display: flex;
        }

        .modal-content {
            background: white;
            border-radius: 16px;
            padding: 24px;
            max-width: 500px;
            width: 100%;
            max-height: 90vh;
            overflow-y: auto;
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .modal-title {
            font-size: 20px;
            font-weight: 700;
        }

        .modal-close {
            background: none;
            border: none;
            font-size: 24px;
            cursor: pointer;
            color: #999;
        }

        .modal-close:hover {
            color: #333;
        }

        .modal-buttons {
            display: flex;
            gap: 12px;
            margin-top: 20px;
        }

        .modal-buttons .btn {
            flex: 1;
        }

        /* Time Grid */
        .time-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 8px;
            margin-top: 12px;
        }

        .time-slot {
            padding: 12px 8px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            text-align: center;
            cursor: pointer;
            transition: all 0.2s;
            font-size: 14px;
        }

        .time-slot:hover {
            border-color: #667eea;
        }

        .time-slot.selected {
            background: #667eea;
            border-color: #667eea;
            color: white;
        }

        .time-slot.unavailable {
            background: #f5f5f5;
            color: #999;
            cursor: not-allowed;
        }

        /* Loading */
        .loading-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255,255,255,0.9);
            z-index: 2000;
            align-items: center;
            justify-content: center;
            flex-direction: column;
        }

        .loading-overlay.show {
            display: flex;
        }

        .spinner {
            width: 50px;
            height: 50px;
            border: 4px solid #e0e0e0;
            border-top-color: #667eea;
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .loading-text {
            margin-top: 16px;
            font-size: 16px;
            color: #666;
        }

        /* Toast */
        .toast {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%) translateY(100px);
            background: #333;
            color: white;
            padding: 16px 24px;
            border-radius: 10px;
            font-size: 14px;
            z-index: 3000;
            transition: transform 0.3s;
            max-width: 90%;
        }

        .toast.show {
            transform: translateX(-50%) translateY(0);
        }

        .toast.success {
            background: #2e7d32;
        }

        .toast.error {
            background: #c62828;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 40px 20px;
            color: #999;
        }

        .empty-state-icon {
            font-size: 64px;
            margin-bottom: 16px;
        }

        .empty-state-text {
            font-size: 16px;
        }

        /* Responsive */
        @media (max-width: 600px) {
            .booking-item {
                flex-direction: column;
                align-items: stretch;
            }

            .booking-time {
                width: 100%;
            }

            .booking-actions {
                width: 100%;
            }

            .booking-actions .btn {
                flex: 1;
            }

            .time-grid {
                grid-template-columns: repeat(3, 1fr);
            }

            .package-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }
    </style>
</head>
<body>
    <!-- ============================================
         LOGIN SCREEN
         ============================================ -->
    <div id="loginScreen" class="login-screen">
        <div class="login-box">
            <div class="login-logo">🏓</div>
            <h1 class="login-title">TK Pickleball</h1>
            <p class="login-subtitle">Admin Panel</p>
            
            <div class="pin-display">
                <div class="pin-dot" id="dot1"></div>
                <div class="pin-dot" id="dot2"></div>
                <div class="pin-dot" id="dot3"></div>
                <div class="pin-dot" id="dot4"></div>
            </div>
            
            <div class="pin-pad">
                <button class="pin-btn" onclick="enterPin('1')">1</button>
                <button class="pin-btn" onclick="enterPin('2')">2</button>
                <button class="pin-btn" onclick="enterPin('3')">3</button>
                <button class="pin-btn" onclick="enterPin('4')">4</button>
                <button class="pin-btn" onclick="enterPin('5')">5</button>
                <button class="pin-btn" onclick="enterPin('6')">6</button>
                <button class="pin-btn" onclick="enterPin('7')">7</button>
                <button class="pin-btn" onclick="enterPin('8')">8</button>
                <button class="pin-btn" onclick="enterPin('9')">9</button>
                <button class="pin-btn clear" onclick="clearPin()">C</button>
                <button class="pin-btn" onclick="enterPin('0')">0</button>
                <button class="pin-btn submit" onclick="submitPin()">✓</button>
            </div>
            
            <p id="loginError" class="login-error">Incorrect PIN. Please try again.</p>
        </div>
    </div>

    <!-- ============================================
         MAIN DASHBOARD
         ============================================ -->
    <div id="dashboard" class="dashboard">
        <!-- Header -->
        <div class="header">
            <div>
                <div class="header-title">🏓 TK Pickleball Admin</div>
                <div class="header-staff">Staff: <span id="staffName">Admin</span></div>
            </div>
            <button class="logout-btn" onclick="logout()">Logout</button>
        </div>

        <!-- Navigation Tabs -->
        <div class="nav-tabs">
            <button class="nav-tab active" data-tab="bookings" onclick="switchTab('bookings')">
                <span class="nav-tab-icon">📅</span>
                Bookings
            </button>
            <button class="nav-tab" data-tab="credits" onclick="switchTab('credits')">
                <span class="nav-tab-icon">💳</span>
                Credits
            </button>
            <button class="nav-tab" data-tab="customers" onclick="switchTab('customers')">
                <span class="nav-tab-icon">👥</span>
                Customers
            </button>
            <button class="nav-tab" data-tab="stats" onclick="switchTab('stats')">
                <span class="nav-tab-icon">📊</span>
                Stats
            </button>
        </div>

        <!-- ============================================
             BOOKINGS TAB
             ============================================ -->
        <div id="tab-bookings" class="content-section active">
            <div class="date-picker-row">
                <input type="date" id="bookingDate" class="date-input" onchange="loadBookings()">
                <button class="quick-btn active" onclick="setBookingDate(0)">Today</button>
                <button class="quick-btn" onclick="setBookingDate(1)">Tomorrow</button>
                <button class="quick-btn" onclick="setBookingDate(-1)">Yesterday</button>
                <button class="btn btn-primary" onclick="showNewBookingModal()">+ New Booking</button>
            </div>

            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-value" id="statTodayBookings">0</div>
                    <div class="stat-label">Bookings</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value" id="statTodayHours">0</div>
                    <div class="stat-label">Court Hours</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value">$<span id="statTodayRevenue">0</span></div>
                    <div class="stat-label">Revenue</div>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">📅 Bookings</h3>
                    <button class="btn btn-secondary" onclick="loadBookings()">🔄 Refresh</button>
                </div>
                <div id="bookingsList" class="booking-list">
                    <div class="empty-state">
                        <div class="empty-state-icon">📅</div>
                        <p class="empty-state-text">No bookings for this date</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- ============================================
             CREDITS TAB
             ============================================ -->
        <div id="tab-credits" class="content-section">
            <div class="card">
                <h3 class="card-title" style="margin-bottom: 16px;">🔍 Find Customer</h3>
                <div class="search-box">
                    <input type="text" id="creditSearchInput" class="search-input" placeholder="Enter phone number or name..." onkeypress="if(event.key==='Enter')searchCustomerForCredit()">
                    <button class="search-btn" onclick="searchCustomerForCredit()">🔍 Search</button>
                </div>
            </div>

            <div id="creditCustomerCard" style="display: none;">
                <!-- Customer info will be inserted here -->
            </div>

            <div id="creditPackagesSection" style="display: none;">
                <div class="card">
                    <h3 class="card-title" style="margin-bottom: 16px;">📦 Select Package</h3>
                    <div class="package-grid" id="packageGrid">
                        <!-- Packages will be inserted here -->
                    </div>
                </div>

                <div class="card">
                    <h3 class="card-title" style="margin-bottom: 16px;">📝 Summary</h3>
                    <div class="summary-box">
                        <div class="summary-row">
                            <span>Package</span>
                            <span id="summaryPackage">-</span>
                        </div>
                        <div class="summary-row">
                            <span>Hours</span>
                            <span id="summaryHours">-</span>
                        </div>
                        <div class="summary-row">
                            <span>Current Balance</span>
                            <span id="summaryCurrentBalance">-</span>
                        </div>
                        <div class="summary-row">
                            <span>New Balance</span>
                            <span id="summaryNewBalance">-</span>
                        </div>
                        <div class="summary-row">
                            <span>Amount to Collect</span>
                            <span>$<span id="summaryPrice">0</span></span>
                        </div>
                    </div>
                    <button class="btn btn-primary btn-large" onclick="addCreditsToCustomer()">✅ Add Credits</button>
                </div>
            </div>

            <div id="newCustomerSection" class="card" style="display: none;">
                <h3 class="card-title" style="margin-bottom: 16px;">🆕 New Customer</h3>
                <p style="color: #666; margin-bottom: 16px;">Customer not found. Create new credit account:</p>
                <div class="form-group">
                    <label class="form-label">Name *</label>
                    <input type="text" id="newCustomerName" class="form-input" placeholder="Customer name">
                </div>
                <div class="form-group">
                    <label class="form-label">Phone *</label>
                    <input type="tel" id="newCustomerPhone" class="form-input" placeholder="Phone number">
                </div>
                <button class="btn btn-success" onclick="createNewCustomer()">Create Customer</button>
            </div>
        </div>

        <!-- ============================================
             CUSTOMERS TAB
             ============================================ -->
        <div id="tab-customers" class="content-section">
            <div class="card">
                <div class="search-box">
                    <input type="text" id="customerSearchInput" class="search-input" placeholder="Search by name or phone..." onkeypress="if(event.key==='Enter')searchCustomers()">
                    <button class="search-btn" onclick="searchCustomers()">🔍 Search</button>
                </div>
            </div>

            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">👥 Customers with Credits</h3>
                </div>
                <div id="customersList" class="customer-list">
                    <div class="empty-state">
                        <div class="empty-state-icon">👥</div>
                        <p class="empty-state-text">Loading customers...</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- ============================================
             STATS TAB
             ============================================ -->
        <div id="tab-stats" class="content-section">
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-value" id="statTotalBookings">0</div>
                    <div class="stat-label">Total Bookings (30d)</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value">$<span id="statTotalRevenue">0</span></div>
                    <div class="stat-label">Total Revenue (30d)</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value" id="statTotalMembers">0</div>
                    <div class="stat-label">Credit Members</div>
                </div>
                <div class="stat-card">
                    <div class="stat-value" id="statTotalCreditHours">0</div>
                    <div class="stat-label">Credit Hours Sold</div>
                </div>
            </div>

            <div class="card">
                <h3 class="card-title" style="margin-bottom: 16px;">📦 Credit Packages Sold</h3>
                <div id="packageStats">
                    <!-- Package stats will be inserted here -->
                </div>
            </div>

            <div class="card">
                <h3 class="card-title" style="margin-bottom: 16px;">🏆 Top Customers</h3>
                <div id="topCustomers">
                    <!-- Top customers will be inserted here -->
                </div>
            </div>
        </div>
    </div>

    <!-- ============================================
         NEW BOOKING MODAL
         ============================================ -->
    <div id="newBookingModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">📅 New Booking</h2>
                <button class="modal-close" onclick="hideModal('newBookingModal')">&times;</button>
            </div>

            <div class="form-group">
                <label class="form-label">Customer Name *</label>
                <input type="text" id="newBookingName" class="form-input" placeholder="Enter name">
            </div>

            <div class="form-group">
                <label class="form-label">Phone *</label>
                <input type="tel" id="newBookingPhone" class="form-input" placeholder="Enter phone">
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label class="form-label">Date *</label>
                    <input type="date" id="newBookingDate" class="form-input">
                </div>
                <div class="form-group">
                    <label class="form-label">Duration *</label>
                    <select id="newBookingDuration" class="form-select" onchange="updateNewBookingPrice()">
                        <option value="1">1 Hour</option>
                        <option value="2">2 Hours</option>
                        <option value="3">3 Hours</option>
                        <option value="4">4 Hours</option>
                    </select>
                </div>
            </div>

            <div class="form-group">
                <label class="form-label">Courts *</label>
                <select id="newBookingCourts" class="form-select" onchange="updateNewBookingPrice()">
                    <option value="1">1 Court</option>
                    <option value="2">2 Courts</option>
                    <option value="3">3 Courts</option>
                    <option value="4">4 Courts</option>
                </select>
            </div>

            <div class="form-group">
                <label class="form-label">Start Time *</label>
                <div id="newBookingTimeGrid" class="time-grid">
                    <!-- Time slots will be inserted here -->
                </div>
                <input type="hidden" id="newBookingTime">
            </div>

            <div class="form-group">
                <label class="form-label">Payment Method</label>
                <select id="newBookingPayment" class="form-select">
                    <option value="cash">💵 Cash (Pay on Arrival)</option>
                    <option value="credits">🎫 Credits</option>
                </select>
            </div>

            <div class="summary-box">
                <div class="summary-row">
                    <span>Duration</span>
                    <span><span id="newBookingSummaryDuration">1</span> hour(s)</span>
                </div>
                <div class="summary-row">
                    <span>Courts</span>
                    <span><span id="newBookingSummaryCourts">1</span> court(s)</span>
                </div>
                <div class="summary-row">
                    <span>Total</span>
                    <span>$<span id="newBookingPrice">15</span></span>
                </div>
            </div>

            <div class="modal-buttons">
                <button class="btn btn-secondary" onclick="hideModal('newBookingModal')">Cancel</button>
                <button class="btn btn-primary" onclick="createNewBooking()">Create Booking</button>
            </div>
        </div>
    </div>

    <!-- ============================================
         CANCEL BOOKING MODAL
         ============================================ -->
    <div id="cancelBookingModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">❌ Cancel Booking</h2>
                <button class="modal-close" onclick="hideModal('cancelBookingModal')">&times;</button>
            </div>

            <p style="margin-bottom: 16px;">Are you sure you want to cancel this booking?</p>

            <div class="summary-box" id="cancelBookingDetails">
                <!-- Booking details will be inserted here -->
            </div>

            <div id="cancelRefundInfo" style="background: #e8f5e9; padding: 12px; border-radius: 8px; margin-bottom: 16px; display: none;">
                <strong>💳 Credit Refund:</strong> <span id="cancelRefundAmount">0</span> hours will be refunded to the customer.
            </div>

            <div class="modal-buttons">
                <button class="btn btn-secondary" onclick="hideModal('cancelBookingModal')">Keep Booking</button>
                <button class="btn btn-danger" onclick="confirmCancelBooking()">Cancel Booking</button>
            </div>
        </div>
    </div>

    <!-- ============================================
         VIEW CUSTOMER MODAL
         ============================================ -->
    <div id="viewCustomerModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title">👤 Customer Details</h2>
                <button class="modal-close" onclick="hideModal('viewCustomerModal')">&times;</button>
            </div>

            <div id="viewCustomerContent">
                <!-- Customer details will be inserted here -->
            </div>

            <div class="modal-buttons">
                <button class="btn btn-secondary" onclick="hideModal('viewCustomerModal')">Close</button>
                <button class="btn btn-primary" onclick="addCreditsFromCustomerView()">Add Credits</button>
            </div>
        </div>
    </div>

    <!-- ============================================
         LOADING OVERLAY
         ============================================ -->
    <div id="loadingOverlay" class="loading-overlay">
        <div class="spinner"></div>
        <p class="loading-text">Loading...</p>
    </div>

    <!-- ============================================
         TOAST
         ============================================ -->
    <div id="toast" class="toast"></div>

    <script>
        // ============================================
        // CONFIGURATION
        // ============================================
        const BACKEND_URL = 'https://script.google.com/macros/s/AKfycbySlkS8Yb3ACIfh5izbul6FfwTxgzrWg4_SnEMHsC21JyiaC1V9fak4GQFP4w3FvyjYPw/exec';
        const ADMIN_PIN = '1234'; // Change this to your desired PIN
        const PRICE_PER_HOUR = 15;

        const PACKAGES = [
            { id: 'starter', name: 'Starter', hours: 5, price: 65 },
            { id: 'value', name: 'Value', hours: 10, price: 120 },
            { id: 'premium', name: 'Premium', hours: 20, price: 220 },
            { id: 'ultimate', name: 'Ultimate', hours: 40, price: 400 }
        ];

        const TIME_SLOTS = [
            { value: '08:00', label: '8 AM' },
            { value: '09:00', label: '9 AM' },
            { value: '10:00', label: '10 AM' },
            { value: '11:00', label: '11 AM' },
            { value: '12:00', label: '12 PM' },
            { value: '13:00', label: '1 PM' },
            { value: '14:00', label: '2 PM' },
            { value: '15:00', label: '3 PM' },
            { value: '16:00', label: '4 PM' },
            { value: '17:00', label: '5 PM' },
            { value: '18:00', label: '6 PM' },
            { value: '19:00', label: '7 PM' },
            { value: '20:00', label: '8 PM' },
            { value: '21:00', label: '9 PM' }
        ];

        // State
        let currentPin = '';
        let selectedCustomer = null;
        let selectedPackage = null;
        let bookingToCancel = null;
        let viewingCustomer = null;

        // ============================================
        // LOGIN FUNCTIONS
        // ============================================
        function enterPin(num) {
            if (currentPin.length < 4) {
                currentPin += num;
                updatePinDisplay();
            }
        }

        function clearPin() {
            currentPin = '';
            updatePinDisplay();
            document.getElementById('loginError').classList.remove('show');
        }

        function updatePinDisplay() {
            for (let i = 1; i <= 4; i++) {
                const dot = document.getElementById('dot' + i);
                dot.classList.remove('filled', 'error');
                if (i <= currentPin.length) {
                    dot.classList.add('filled');
                }
            }
        }

        function submitPin() {
            if (currentPin.length !== 4) return;

            if (currentPin === ADMIN_PIN) {
                // Success
                document.getElementById('loginScreen').style.display = 'none';
                document.getElementById('dashboard').classList.add('show');
                localStorage.setItem('adminLoggedIn', 'true');
                initDashboard();
            } else {
                // Error
                for (let i = 1; i <= 4; i++) {
                    document.getElementById('dot' + i).classList.add('error');
                }
                document.getElementById('loginError').classList.add('show');
                setTimeout(clearPin, 500);
            }
        }

        function logout() {
            localStorage.removeItem('adminLoggedIn');
            document.getElementById('dashboard').classList.remove('show');
            document.getElementById('loginScreen').style.display = 'flex';
            clearPin();
        }

        // ============================================
        // DASHBOARD FUNCTIONS
        // ============================================
        function initDashboard() {
            // Set today's date
            const today = formatDate(new Date());
            document.getElementById('bookingDate').value = today;
            document.getElementById('newBookingDate').value = today;

            // Load initial data
            loadBookings();
            loadCustomers();
            loadStats();
            renderPackages();
            renderNewBookingTimeSlots();
        }

        function switchTab(tabId) {
            // Update tab buttons
            document.querySelectorAll('.nav-tab').forEach(tab => {
                tab.classList.toggle('active', tab.dataset.tab === tabId);
            });

            // Update content sections
            document.querySelectorAll('.content-section').forEach(section => {
                section.classList.toggle('active', section.id === 'tab-' + tabId);
            });

            // Load data for specific tabs
            if (tabId === 'customers') {
                loadCustomers();
            } else if (tabId === 'stats') {
                loadStats();
            }
        }

        // ============================================
        // BOOKINGS FUNCTIONS
        // ============================================
        function setBookingDate(offset) {
            const date = new Date();
            date.setDate(date.getDate() + offset);
            document.getElementById('bookingDate').value = formatDate(date);

            // Update quick buttons
            document.querySelectorAll('.date-picker-row .quick-btn').forEach(btn => {
                btn.classList.remove('active');
            });
            event.target.classList.add('active');

            loadBookings();
        }

        async function loadBookings() {
            const date = document.getElementById('bookingDate').value;
            if (!date) return;

            showLoading(true);

            try {
                const url = `${BACKEND_URL}?action=getBookings&date=${date}`;
                const response = await fetch(url);
                const data = await response.json();

                if (data.success) {
                    renderBookings(data.bookings || []);
                    updateBookingStats(data.bookings || []);
                } else {
                    showToast(data.error || 'Failed to load bookings', 'error');
                }
            } catch (error) {
                console.error('Error loading bookings:', error);
                showToast('Connection error', 'error');
            }

            showLoading(false);
        }

        function renderBookings(bookings) {
            const container = document.getElementById('bookingsList');

            if (bookings.length === 0) {
                container.innerHTML = `
                    <div class="empty-state">
                        <div class="empty-state-icon">📅</div>
                        <p class="empty-state-text">No bookings for this date</p>
                    </div>
                `;
                return;
            }

            // Sort by time
            bookings.sort((a, b) => a.time.localeCompare(b.time));

            container.innerHTML = bookings.map(booking => {
                const timeLabel = TIME_SLOTS.find(t => t.value === booking.time)?.label || booking.time;
                const paymentBadge = booking.payment_method === 'credits' 
                    ? '<span class="booking-badge badge-credits">🎫 Credits</span>'
                    : '<span class="booking-badge badge-cash">💵 Cash</span>';
                const statusBadge = booking.status === 'cancelled'
                    ? '<span class="booking-badge badge-cancelled">Cancelled</span>'
                    : '';

                return `
                    <div class="booking-item" data-booking-id="${booking.booking_id}">
                        <div class="booking-time">${timeLabel}</div>
                        <div class="booking-info">
                            <div class="booking-name">${booking.name}</div>
                            <div class="booking-details">
                                📞 ${booking.phone} • 🎾 ${booking.courts} court(s) • ⏱️ ${booking.duration} hr(s)
                            </div>
                        </div>
                        <div class="booking-meta">
                            ${paymentBadge}
                            ${statusBadge}
                        </div>
                        <div class="booking-actions">
                            ${booking.status !== 'cancelled' ? `
                                <button class="btn btn-danger" onclick="showCancelBookingModal('${booking.booking_id}', '${booking.name}', '${booking.time}', ${booking.duration}, ${booking.courts}, '${booking.payment_method}', '${booking.telegram_id || ''}')">Cancel</button>
                            ` : ''}
                        </div>
                    </div>
                `;
            }).join('');
        }

        function updateBookingStats(bookings) {
            const activeBookings = bookings.filter(b => b.status !== 'cancelled');
            const totalBookings = activeBookings.length;
            const totalHours = activeBookings.reduce((sum, b) => sum + (b.duration * b.courts), 0);
            const cashBookings = activeBookings.filter(b => b.payment_method !== 'credits');
            const totalRevenue = cashBookings.reduce((sum, b) => sum + (b.duration * b.courts * PRICE_PER_HOUR), 0);

            document.getElementById('statTodayBookings').textContent = totalBookings;
            document.getElementById('statTodayHours').textContent = totalHours;
            document.getElementById('statTodayRevenue').textContent = totalRevenue;
        }

        // ============================================
        // NEW BOOKING FUNCTIONS
        // ============================================
        function showNewBookingModal() {
            document.getElementById('newBookingDate').value = document.getElementById('bookingDate').value;
            document.getElementById('newBookingName').value = '';
            document.getElementById('newBookingPhone').value = '';
            document.getElementById('newBookingDuration').value = '1';
            document.getElementById('newBookingCourts').value = '1';
            document.getElementById('newBookingTime').value = '';
            document.getElementById('newBookingPayment').value = 'cash';
            
            renderNewBookingTimeSlots();
            updateNewBookingPrice();
            showModal('newBookingModal');
        }

        function renderNewBookingTimeSlots() {
            const container = document.getElementById('newBookingTimeGrid');
            container.innerHTML = TIME_SLOTS.map(slot => `
                <div class="time-slot" data-time="${slot.value}" onclick="selectNewBookingTime('${slot.value}')">
                    ${slot.label}
                </div>
            `).join('');
        }

        function selectNewBookingTime(time) {
            document.querySelectorAll('#newBookingTimeGrid .time-slot').forEach(slot => {
                slot.classList.toggle('selected', slot.dataset.time === time);
            });
            document.getElementById('newBookingTime').value = time;
        }

        function updateNewBookingPrice() {
            const duration = parseInt(document.getElementById('newBookingDuration').value);
            const courts = parseInt(document.getElementById('newBookingCourts').value);
            const total = duration * courts * PRICE_PER_HOUR;

            document.getElementById('newBookingSummaryDuration').textContent = duration;
            document.getElementById('newBookingSummaryCourts').textContent = courts;
            document.getElementById('newBookingPrice').textContent = total;
        }

        async function createNewBooking() {
            const name = document.getElementById('newBookingName').value.trim();
            const phone = document.getElementById('newBookingPhone').value.trim();
            const date = document.getElementById('newBookingDate').value;
            const time = document.getElementById('newBookingTime').value;
            const duration = document.getElementById('newBookingDuration').value;
            const courts = document.getElementById('newBookingCourts').value;
            const payment = document.getElementById('newBookingPayment').value;

            if (!name || !phone || !date || !time) {
                showToast('Please fill all required fields', 'error');
                return;
            }

            showLoading(true);

            try {
                const params = new URLSearchParams({
                    action: 'book',
                    name: name,
                    phone: phone,
                    date: date,
                    time: time,
                    duration: duration,
                    courts: courts,
                    payment_method: payment,
                    source: 'admin'
                });

                const url = `${BACKEND_URL}?${params.toString()}`;
                const response = await fetch(url);
                const data = await response.json();

                if (data.success || data.status === 'success') {
                    showToast('Booking created successfully!', 'success');
                    hideModal('newBookingModal');
                    loadBookings();
                } else {
                    showToast(data.error || 'Failed to create booking', 'error');
                }
            } catch (error) {
                console.error('Error creating booking:', error);
                showToast('Connection error', 'error');
            }

            showLoading(false);
        }

        // ============================================
        // CANCEL BOOKING FUNCTIONS
        // ============================================
        function showCancelBookingModal(bookingId, name, time, duration, courts, paymentMethod, telegramId) {
            bookingToCancel = { bookingId, name, time, duration, courts, paymentMethod, telegramId };

            const timeLabel = TIME_SLOTS.find(t => t.value === time)?.label || time;
            const totalHours = duration * courts;

            document.getElementById('cancelBookingDetails').innerHTML = `
                <div class="summary-row"><span>Name</span><span>${name}</span></div>
                <div class="summary-row"><span>Time</span><span>${timeLabel}</span></div>
                <div class="summary-row"><span>Duration</span><span>${duration} hour(s)</span></div>
                <div class="summary-row"><span>Courts</span><span>${courts}</span></div>
                <div class="summary-row"><span>Payment</span><span>${paymentMethod === 'credits' ? '🎫 Credits' : '💵 Cash'}</span></div>
            `;

            const refundInfo = document.getElementById('cancelRefundInfo');
            if (paymentMethod === 'credits') {
                refundInfo.style.display = 'block';
                document.getElementById('cancelRefundAmount').textContent = totalHours;
            } else {
                refundInfo.style.display = 'none';
            }

            showModal('cancelBookingModal');
        }

        async function confirmCancelBooking() {
            if (!bookingToCancel) return;

            showLoading(true);

            try {
                const params = new URLSearchParams({
                    action: 'cancelBookingAdmin',
                    booking_id: bookingToCancel.bookingId,
                    refund_credits: bookingToCancel.paymentMethod === 'credits' ? 'true' : 'false',
                    telegram_id: bookingToCancel.telegramId || ''
                });

                const url = `${BACKEND_URL}?${params.toString()}`;
                const response = await fetch(url);
                const data = await response.json();

                if (data.success) {
                    showToast('Booking cancelled successfully', 'success');
                    hideModal('cancelBookingModal');
                    loadBookings();
                } else {
                    showToast(data.error || 'Failed to cancel booking', 'error');
                }
            } catch (error) {
                console.error('Error cancelling booking:', error);
                showToast('Connection error', 'error');
            }

            showLoading(false);
            bookingToCancel = null;
        }

        // ============================================
        // CREDITS FUNCTIONS
        // ============================================
        async function searchCustomerForCredit() {
            const searchTerm = document.getElementById('creditSearchInput').value.trim();
            if (!searchTerm) {
                showToast('Please enter a phone number or name', 'error');
                return;
            }

            showLoading(true);

            try {
                const url = `${BACKEND_URL}?action=searchCreditsByPhone&phone=${encodeURIComponent(searchTerm)}`;
                const response = await fetch(url);
                const data = await response.json();

                if (data.success && data.results && data.results.length > 0) {
                    selectedCustomer = data.results[0];
                    renderCustomerCard(selectedCustomer);
                    document.getElementById('creditCustomerCard').style.display = 'block';
                    document.getElementById('creditPackagesSection').style.display = 'block';
                    document.getElementById('newCustomerSection').style.display = 'none';
                    
                    // Reset package selection
                    selectedPackage = null;
                    document.querySelectorAll('.package-card').forEach(c => c.classList.remove('selected'));
                    updateCreditSummary();
                } else {
                    // No customer found - show new customer form
                    selectedCustomer = null;
                    document.getElementById('creditCustomerCard').style.display = 'none';
                    document.getElementById('creditPackagesSection').style.display = 'none';
                    document.getElementById('newCustomerSection').style.display = 'block';
                    document.getElementById('newCustomerPhone').value = searchTerm;
                }
            } catch (error) {
                console.error('Error searching customer:', error);
                showToast('Connection error', 'error');
            }

            showLoading(false);
        }

        function renderCustomerCard(customer) {
            const container = document.getElementById('creditCustomerCard');
            const statusText = customer.isLinked ? 'Telegram Connected ✅' : 'Pending Activation ⏳';

            container.innerHTML = `
                <div class="customer-card">
                    <div class="customer-header">
                        <div>
                            <div class="customer-name">${customer.name || 'Unknown'}</div>
                            <div class="customer-phone">📞 ${customer.phone}</div>
                            <div class="customer-status">${statusText}</div>
                        </div>
                        <div class="customer-balance">
                            <div class="customer-balance-label">Credit Balance</div>
                            <div class="customer-balance-value">${customer.balance || 0} hrs</div>
                        </div>
                    </div>
                </div>
            `;
        }

        function renderPackages() {
            const container = document.getElementById('packageGrid');
            container.innerHTML = PACKAGES.map(pkg => `
                <div class="package-card" data-package="${pkg.id}" onclick="selectPackage('${pkg.id}')">
                    <div class="package-name">${pkg.name}</div>
                    <div class="package-hours">${pkg.hours}</div>
                    <div class="package-hours-label">hours</div>
                    <div class="package-price">$${pkg.price}</div>
                </div>
            `).join('');
        }

        function selectPackage(packageId) {
            selectedPackage = PACKAGES.find(p => p.id === packageId);
            
            document.querySelectorAll('.package-card').forEach(card => {
                card.classList.toggle('selected', card.dataset.package === packageId);
            });

            updateCreditSummary();
        }

        function updateCreditSummary() {
            if (!selectedPackage) {
                document.getElementById('summaryPackage').textContent = '-';
                document.getElementById('summaryHours').textContent = '-';
                document.getElementById('summaryCurrentBalance').textContent = '-';
                document.getElementById('summaryNewBalance').textContent = '-';
                document.getElementById('summaryPrice').textContent = '0';
                return;
            }

            const currentBalance = selectedCustomer?.balance || 0;
            const newBalance = currentBalance + selectedPackage.hours;

            document.getElementById('summaryPackage').textContent = selectedPackage.name;
            document.getElementById('summaryHours').textContent = selectedPackage.hours + ' hours';
            document.getElementById('summaryCurrentBalance').textContent = currentBalance + ' hours';
            document.getElementById('summaryNewBalance').textContent = newBalance + ' hours';
            document.getElementById('summaryPrice').textContent = selectedPackage.price;
        }

        async function addCreditsToCustomer() {
            if (!selectedCustomer || !selectedPackage) {
                showToast('Please select a customer and package', 'error');
                return;
            }

            showLoading(true);

            try {
                const params = new URLSearchParams({
                    action: 'addCredits',
                    phone: selectedCustomer.phone,
                    name: selectedCustomer.name || 'Customer',
                    package: selectedPackage.id,
                    staff: document.getElementById('staffName').textContent
                });

                const url = `${BACKEND_URL}?${params.toString()}`;
                const response = await fetch(url);
                const data = await response.json();

                if (data.success) {
                    let message = `✅ ${selectedPackage.hours} hours added!`;
                    if (data.activationCode) {
                        message += `\n\n📝 Activation Code: ${data.activationCode}`;
                    }
                    showToast(message, 'success');

                    // Reset form
                    document.getElementById('creditSearchInput').value = '';
                    document.getElementById('creditCustomerCard').style.display = 'none';
                    document.getElementById('creditPackagesSection').style.display = 'none';
                    selectedCustomer = null;
                    selectedPackage = null;

                    // Show activation code in alert if new customer
                    if (data.activationCode) {
                        alert(`ACTIVATION CODE: ${data.activationCode}\n\nGive this code to the customer.\nThey enter it in the Telegram bot to activate their credits.`);
                    }
                } else {
                    showToast(data.error || 'Failed to add credits', 'error');
                }
            } catch (error) {
                console.error('Error adding credits:', error);
                showToast('Connection error', 'error');
            }

            showLoading(false);
        }

        async function createNewCustomer() {
            const name = document.getElementById('newCustomerName').value.trim();
            const phone = document.getElementById('newCustomerPhone').value.trim();

            if (!name || !phone) {
                showToast('Please enter name and phone', 'error');
                return;
            }

            // Set as selected customer and show packages
            selectedCustomer = { name, phone, balance: 0, isLinked: false };
            renderCustomerCard(selectedCustomer);
            document.getElementById('creditCustomerCard').style.display = 'block';
            document.getElementById('creditPackagesSection').style.display = 'block';
            document.getElementById('newCustomerSection').style.display = 'none';

            showToast('Customer ready. Now select a package.', 'success');
        }

        // ============================================
        // CUSTOMERS FUNCTIONS
        // ============================================
        async function loadCustomers() {
            showLoading(true);

            try {
                const url = `${BACKEND_URL}?action=getAllCredits`;
                const response = await fetch(url);
                const data = await response.json();

                if (data.success) {
                    renderCustomersList(data.customers || []);
                }
            } catch (error) {
                console.error('Error loading customers:', error);
            }

            showLoading(false);
        }

        async function searchCustomers() {
            const searchTerm = document.getElementById('customerSearchInput').value.trim();
            
            if (!searchTerm) {
                loadCustomers();
                return;
            }

            showLoading(true);

            try {
                const url = `${BACKEND_URL}?action=searchCreditsByPhone&phone=${encodeURIComponent(searchTerm)}`;
                const response = await fetch(url);
                const data = await response.json();

                if (data.success) {
                    renderCustomersList(data.results || []);
                }
            } catch (error) {
                console.error('Error searching customers:', error);
            }

            showLoading(false);
        }

        function renderCustomersList(customers) {
            const container = document.getElementById('customersList');

            if (customers.length === 0) {
                container.innerHTML = `
                    <div class="empty-state">
                        <div class="empty-state-icon">👥</div>
                        <p class="empty-state-text">No customers found</p>
                    </div>
                `;
                return;
            }

            container.innerHTML = customers.map(customer => `
                <div class="customer-list-item" onclick="showCustomerDetails('${customer.phone}', '${customer.name || ''}', ${customer.balance || 0}, '${customer.telegramId || ''}', ${customer.totalPurchased || 0}, ${customer.totalUsed || 0})">
                    <div class="customer-list-info">
                        <div class="customer-list-name">${customer.name || 'Unknown'}</div>
                        <div class="customer-list-phone">📞 ${customer.phone} ${customer.isLinked ? '✅' : '⏳'}</div>
                    </div>
                    <div class="customer-list-balance">
                        <div class="customer-list-balance-value">${customer.balance || 0}</div>
                        <div class="customer-list-balance-label">hours</div>
                    </div>
                </div>
            `).join('');
        }

        function showCustomerDetails(phone, name, balance, telegramId, totalPurchased, totalUsed) {
            viewingCustomer = { phone, name, balance, telegramId, totalPurchased, totalUsed };

            document.getElementById('viewCustomerContent').innerHTML = `
                <div class="customer-card" style="margin-bottom: 16px;">
                    <div class="customer-header">
                        <div>
                            <div class="customer-name">${name || 'Unknown'}</div>
                            <div class="customer-phone">📞 ${phone}</div>
                            <div class="customer-status">${telegramId ? 'Telegram Connected ✅' : 'Pending Activation ⏳'}</div>
                        </div>
                        <div class="customer-balance">
                            <div class="customer-balance-label">Balance</div>
                            <div class="customer-balance-value">${balance} hrs</div>
                        </div>
                    </div>
                </div>
                <div class="summary-box">
                    <div class="summary-row"><span>Total Purchased</span><span>${totalPurchased} hours</span></div>
                    <div class="summary-row"><span>Total Used</span><span>${totalUsed} hours</span></div>
                    <div class="summary-row"><span>Current Balance</span><span>${balance} hours</span></div>
                </div>
            `;

            showModal('viewCustomerModal');
        }

        function addCreditsFromCustomerView() {
            if (viewingCustomer) {
                hideModal('viewCustomerModal');
                switchTab('credits');
                document.getElementById('creditSearchInput').value = viewingCustomer.phone;
                searchCustomerForCredit();
            }
        }

        // ============================================
        // STATS FUNCTIONS
        // ============================================
        async function loadStats() {
            showLoading(true);

            try {
                const url = `${BACKEND_URL}?action=getDashboardStats`;
                const response = await fetch(url);
                const data = await response.json();

                if (data.success) {
                    document.getElementById('statTotalBookings').textContent = data.totalBookings || 0;
                    document.getElementById('statTotalRevenue').textContent = data.totalRevenue || 0;
                    document.getElementById('statTotalMembers').textContent = data.totalMembers || 0;
                    document.getElementById('statTotalCreditHours').textContent = data.totalCreditHours || 0;

                    // Render package stats
                    if (data.packageStats) {
                        renderPackageStats(data.packageStats);
                    }

                    // Render top customers
                    if (data.topCustomers) {
                        renderTopCustomers(data.topCustomers);
                    }
                }
            } catch (error) {
                console.error('Error loading stats:', error);
            }

            showLoading(false);
        }

        function renderPackageStats(stats) {
            const container = document.getElementById('packageStats');
            container.innerHTML = Object.entries(stats).map(([name, count]) => `
                <div class="summary-row">
                    <span>📦 ${name}</span>
                    <span>${count} sold</span>
                </div>
            `).join('') || '<p style="color: #999;">No data available</p>';
        }

        function renderTopCustomers(customers) {
            const container = document.getElementById('topCustomers');
            container.innerHTML = customers.map((customer, index) => `
                <div class="summary-row">
                    <span>${index + 1}. ${customer.name}</span>
                    <span>${customer.bookings} bookings</span>
                </div>
            `).join('') || '<p style="color: #999;">No data available</p>';
        }

        // ============================================
        // UTILITY FUNCTIONS
        // ============================================
        function formatDate(date) {
            const d = new Date(date);
            const year = d.getFullYear();
            const month = String(d.getMonth() + 1).padStart(2, '0');
            const day = String(d.getDate()).padStart(2, '0');
            return `${year}-${month}-${day}`;
        }

        function showModal(modalId) {
            document.getElementById(modalId).classList.add('show');
        }

        function hideModal(modalId) {
            document.getElementById(modalId).classList.remove('show');
        }

        function showLoading(show) {
            document.getElementById('loadingOverlay').classList.toggle('show', show);
        }

        function showToast(message, type = 'info') {
            const toast = document.getElementById('toast');
            toast.textContent = message;
            toast.className = `toast ${type} show`;

            setTimeout(() => {
                toast.classList.remove('show');
            }, 3000);
        }

        // ============================================
        // INITIALIZATION
        // ============================================
        document.addEventListener('DOMContentLoaded', function() {
            // Check if already logged in
            if (localStorage.getItem('adminLoggedIn') === 'true') {
                document.getElementById('loginScreen').style.display = 'none';
                document.getElementById('dashboard').classList.add('show');
                initDashboard();
            }
        });
    </script>
</body>
</html>
